# dsh-desktop-pets bundle patch（占位声明）
#
# 启用方式：在 profile 的 cordis.patch.yml 中 insert（id: dsh-desktop-pets），
# 由 install.sh 或 dsh plugin add 处理。此文件声明 dsh.bundle manifest，
# 使本插件可被 dsh plugin add 标准安装。

# 空 patch：插件由 profile 的 cordis.patch.yml insert 启用；保留为合法空数组以便 dsh 加载。
[]
